

function swap(array, i, j) {
	// write a helper function that will swap two values in an array
}


function bubbleSort(array) {
	// write a loop that sweeps across the array over and over, constantly swapping
	// neighboring numbers that are out of order
}

function selectionSort(array) {
	// write a loop that sweeps across the array over and over, finding the smallest
	// element and swapping it to the beginning of the array
}


var array = [12, 1, 5, 17, 23, 5, 2, 8, 4, 2, 6, 22, 110, 92, 46]


// run "node bubbleSort.js" to test your code!
console.log(bubbleSort(array));
//console.log(selectionSort(array));